# Versions used for the reported results (single NVIDIA A100, CUDA 12.x).
# Relax the pins if your platform requires it; results are not sensitive to
# patch versions, but timm >= 1.0.9 is needed for Virchow2's SwiGLUPacked.
torch==2.4.0
timm==1.0.9
webdataset==0.2.86
huggingface_hub==0.24.6
einops==0.8.0
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.14.0
matplotlib==3.9.2
tqdm==4.66.5
synapseclient==4.4.0
